<UserControl x:Class="IntelOrca.Biohazard.HitScan.WpEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntelOrca.Biohazard.HitScan"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:WeaponData}"
             d:DesignWidth="256"
             HorizontalAlignment="Stretch">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Margin="2" Text="{Binding Name}" />
        <TextBox Grid.Column="1" Margin="2" Text="{Binding LowCap}" />
        <TextBox Grid.Column="2" Margin="2" Text="{Binding HighCap}" />
    </Grid>
</UserControl>
