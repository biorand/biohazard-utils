<Window x:Class="IntelOrca.Biohazard.HitScan.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntelOrca.Biohazard.HitScan"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:WeaponListData}"
        Title="HitScan Editor"
        WindowStartupLocation="CenterScreen"
        Width="800"
        x:Name="root">
    <Border Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="3" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="Status">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="0,0,4,0">Game:</TextBlock>
                        <ComboBox Width="128" ItemsSource="{Binding Binders, ElementName=root}" SelectedItem="{Binding SelectedBinder, ElementName=root}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="0,0,4,0">Status:</TextBlock>
                        <TextBlock Text="{Binding ProcessStatus}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>
            <GroupBox Grid.Row="1" Header="Values" Margin="0,0,0,4">
                <ListView ItemsSource="{Binding Weapons}" SelectedItem="{Binding SelectedWeapon}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <local:WpEdit />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>
            <GridSplitter Grid.Row="2" Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <GroupBox Grid.Row="3" Header="Preview">
                <Grid Height="512">
                    <local:WpPreview WeaponData="{Binding SelectedWeapon}" />
                </Grid>
            </GroupBox>
        </Grid>
    </Border>
</Window>
